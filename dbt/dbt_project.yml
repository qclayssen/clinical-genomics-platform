name: cgp_warehouse
version: '1.0.0'
config-version: 2

profile: cgp_warehouse

model-paths: ['models']
test-paths: ['tests']
macro-paths: ['macros']

target-path: 'target'
clean-targets:
  - 'target'
  - 'dbt_packages'

models:
  cgp_warehouse:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: analytics
