# Demo — local reviewer-decision scratch DB (never committed; see ADR-0019)
demo/.local/

# Nextflow
.nextflow/
.nextflow.log*
work/
work_*/
results/
pipeline/assets/reference/*
pipeline/assets/truth/*
pipeline/assets/testdata/*
!pipeline/assets/reference/.gitkeep
!pipeline/assets/truth/.gitkeep
!pipeline/assets/testdata/.gitkeep

# Large data / models
*.fastq.gz
*.bam
*.bai
*.vcf.gz
*.tbi
*.fa
*.fa.*
ai-report/checkpoints/
ai-report/data/*.jsonl

# ...but DO commit the tiny placeholder test data + curated ML sample (small, needed for CI)
!pipeline/assets/testdata/tiny_ref.fa
!pipeline/assets/testdata/tiny_truth.vcf.gz
!pipeline/assets/testdata/tiny_truth.bed
!pipeline/assets/testdata/*.fastq.gz
!ai-report/data/report_pairs.sample.jsonl

# CDK
infra/node_modules/
infra/cdk.out/
infra/cdk.context.json
infra/**/*.js
infra/**/*.d.ts
!infra/jest.config.js

# Python
__pycache__/
*.pyc
venv/
.venv/

# Test / coverage artifacts (regenerated by `pytest --cov`; the SQLite
# .coverage file was previously tracked and produced a spurious diff on
# every local test run)
.coverage
.coverage.*
coverage.xml
htmlcov/
.pytest_cache/

# Secrets / environment
.env
.env.*
*.pem
*.key
credentials.json

# Personal study / interview prep (not part of the project).
# These are private working notes — JOB-PREP-NORTHERN-HEALTH.md says so in its
# own header ("Not for publication — a personal prep doc") — and this is a
# public repo, so they must never be committed.
docs/INTERVIEW-PREP.md
docs/JOB-PREP-NORTHERN-HEALTH.md
docs/INTERVIEW-QA-NORTHERN-HEALTH.md

# Local Claude Code dev-server config: machine-specific (points at .venv paths)
.claude/launch.json

# emr-pipeline — generated SQLite DBs + export output (rebuilt by the scripts,
# see emr-pipeline/README.md)
emr-pipeline/mock_emr/mock_emr.db
emr-pipeline/warehouse.db
emr-pipeline/registry/out/

# OS
.DS_Store
