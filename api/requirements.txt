fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
# Only needed when CGP_DB_URL is set (PostgresRepository):
psycopg[binary]>=3.1,<4.0
# Only needed to run tests/test_api.py (fastapi.testclient.TestClient):
httpx2
