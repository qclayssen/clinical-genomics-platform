# Pinned dependencies for RAG reporter container (Requirement 12.6).
# All versions are exact to ensure reproducible builds.
# Regenerate with: pip-compile requirements.txt --output-file=requirements-lock.txt

# Core ML / inference
torch==2.13.0
transformers==4.44.2
datasets==2.20.0
peft==0.19.1
trl==0.9.6
bitsandbytes==0.49.2
accelerate==0.33.0

# RAG layer
sentence-transformers==2.7.0
faiss-cpu==1.14.3
numpy==1.26.4

# Ollama client (for RAG mode LLM calls)
ollama==0.6.2

# Transitive deps pinned for reproducibility
huggingface-hub==0.24.7
tokenizers==0.23.1
safetensors==0.8.0
tqdm==4.68.4
requests==2.34.2
